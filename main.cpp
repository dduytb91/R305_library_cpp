#include "user.h"

char port[] = "/dev/ttyUSB0";

user user = user(port);

int main()
{
    // printf("Enrolling ID #3\n");
    // getFingerprintEnroll(3);
    // delay(3000);
    // downloadFingerprintTemplate(3);
    // delay(3000);
    // deleteFingerprint(3);

    while (1)
    {
        // delay(50);
        // getFingerprintIDez();
    }
    return 1;
}
